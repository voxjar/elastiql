[package]
name = "elastiql"
version = "0.1.0"
authors = ["William Myers <mwilliammyers@gmail.com>"]
edition = "2018"

[features]
default = ["builder"]

graphql = ["async-graphql"]
builder = ["typed-builder"]

[dependencies]
async-graphql = { version = "1.16.*", optional = true }
base64 = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
typed-builder = { version = "0.6", optional = true }

[dev-dependencies]
elasticsearch = "7.7.1-alpha.1"
tokio = { version = "0.2", default-features = false, features = ["macros", "tcp", "time"] }
